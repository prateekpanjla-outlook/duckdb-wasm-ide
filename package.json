{
  "name": "duckdb-wasm-ide",
  "version": "1.0.0",
  "description": "SQL practice platform with browser-based DuckDB and PostgreSQL-backed question bank",
  "type": "module",
  "scripts": {
    "dev": "cd server && node server.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "lint": "eslint js tests",
    "lint:fix": "eslint js tests --fix"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "eslint": "^8.56.0"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.33.1-dev18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
